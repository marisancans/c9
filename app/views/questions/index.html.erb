<%#= @question.category %>
<% @questions.each do |question| %>
    | <%= question.nr %> | <%= question.correct_answer %> | <%= question.title %><br>
    <ul>
    <% question.answers.each do |answer| %>
      <%=answer.id%> | <%= answer.name %><br>
    <% end %> 
    </ul>
<% end %>